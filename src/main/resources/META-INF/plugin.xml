<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <!-- Unique identifier of the plugin. It should be FQN. It cannot be changed between the plugin versions. -->
    <id>freeriders.mag.Modular-Architecture-Generator</id>

    <!-- Public plugin name should be written in Title Case.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/plugin-overview-page.html#plugin-name -->
    <name>Modular-Architecture-Generator</name>

    <!-- A displayed Vendor name or Organization ID displayed on the Plugins Page. -->
    <vendor>Antoine Maille</vendor>

    <!-- Description of the plugin displayed on the Plugin Page and IDE Plugin Manager.
         Simple HTML elements (text formatting, paragraphs, and lists) can be added inside of <![CDATA[ ]]> tag.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/plugin-overview-page.html#plugin-description -->
    <description><![CDATA[
        <h6>The Modular Architecture Generator allows for creation of complex folder structures based on custom project or user's presets.</h6>

        <p>The presets can be defined for the IDE globally via the settings page or for a specific project via a configuration file called <strong>mag.json</strong> at the root of any project.</p>

        More specifically you can :
        <ul>
            <li>Create user's presets in the settings page</li>
            <li>Select and Export the user's presets as a json file to place in the project</li>
        </ul>

        To be even more modular, you can put the &lt;name&gt; placeholder in any folder or directory name in a preset, to be replaced by the feature name you generate.
    ]]>
    </description>

    <!-- Product and plugin compatibility requirements.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html -->
    <depends>com.intellij.modules.platform</depends>

    <!-- Extension points defined by the plugin.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-extension-points.html -->
    <extensions defaultExtensionNs="com.intellij">
        <notificationGroup id="MAG Notification Group"
                           displayType="BALLOON"/>
    </extensions>
    <actions>
        <group class="freeriders.mag.action.generate.ActionGroupGenerate"
               description="Choose a preset"
               id="freeriders.mag" popup="true"
               text="Generate a Structure">
            <add-to-group anchor="after"
                          group-id="NewGroup" relative-to-action="NewDir" />
        </group>
        <action id="freeriders.mag.action.export.ExportPresetsAction"
                class="freeriders.mag.action.export.ExportPresetsAction" text="ExportPresetsAction"
                description="Export presets as json"/>
    </actions>

    <extensions defaultExtensionNs="com.intellij">
        <applicationService
                serviceImplementation="freeriders.mag.settings.ide.state.AppPresetsState"/>
        <applicationService
                serviceImplementation="freeriders.mag.settings.project.state.ProjectPresetsState"/>
    </extensions>

    <extensions defaultExtensionNs="com.intellij">
        <postStartupActivity implementation="freeriders.mag.settings.project.listener.ProjectManagerOpenedListener"/>
    </extensions>

    <extensions defaultExtensionNs="com.intellij">
        <applicationConfigurable
                parentId="tools"
                instance="freeriders.mag.settings.ide.configurable.AppSettingsConfigurable"
                id="freeriders.mag.settings.ide.configurable.AppSettingsConfigurable"
                displayName="Modular Architecture Generator"/>
    </extensions>
</idea-plugin>